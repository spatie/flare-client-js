// Re-exported so @flareapp/react, @flareapp/svelte and the playgrounds keep importing the browser
// span types from '@flareapp/js/browser'. The values live in core because core's SpanOptions names them.
export { BrowserSpanType } from '@flareapp/core';
